const excResultTotal = document.querySelector('#exc-num-result-total');
const excResultValue = document.querySelector('#exc-num-result-value');
const excResultPercent = document.querySelector('#exc-num-result-percent');
const excResultBox = document.querySelector('.exc-num-result-box');
const excNumContainer = document.querySelector('.exc-num-container');

const excObj = {
    numContainer: excNumContainer,
    resultTotal: excResultTotal,
    resultValue: excResultValue,
    resultPercent: excResultPercent,
    resultBox: excResultBox
};

export default excObj